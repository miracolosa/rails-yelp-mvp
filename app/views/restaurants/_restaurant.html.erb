<h1><%= restaurant.name %></h1>
<p class="mt-2"><strong>Address:</strong> <%= restaurant.address %></p>
<p class="mt-2"><strong>Phone number:</strong> <%= restaurant.phone_number %></p>
<p class="mt-2"><strong>Category:</strong> <%= restaurant.category.capitalize %></p>

<h4 class="mt-4">Reviews for this restaurant:</h4>
<ul>
  <li>
    <% @restaurant.reviews.each do |review| %>
    <%= review.rating %> / <%= review.content %>
    <% end %>
  </li>
</ul>

<%= link_to "Go back to homepage", restaurants_path %>
